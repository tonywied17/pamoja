# pamoja-sensors::ads1115

Generated from rustdoc by `cargo xtask docs` - do not edit by hand.

Texas Instruments ADS1115 16-bit I2C analog-to-digital converter.

The ADS1115 turns an analog signal (a soil-moisture probe, a pH electrode, a
divider) into a 16-bit reading, and a single Config register selects the input,
the gain, the rate, and the comparator. This module builds and parses that Config
register field by field and converts a raw conversion result into a voltage at the
selected full-scale range, following the datasheet's register tables and per-gain
LSB sizes.

## const `CONFIG_RESET`

The power-on value of the Config register (0x8583).

```rust
const CONFIG_RESET: u16
```

## enum `Mux`

The input multiplexer setting (Config bits 14:12).

The ADS1115 measures either of two differential pairs against `AIN3`, the pair
`AIN0`/`AIN1`, or any one input against ground.

- `Ain0Ain1` - AINP = AIN0, AINN = AIN1 (the default).
- `Ain0Ain3` - AINP = AIN0, AINN = AIN3.
- `Ain1Ain3` - AINP = AIN1, AINN = AIN3.
- `Ain2Ain3` - AINP = AIN2, AINN = AIN3.
- `Ain0Gnd` - AINP = AIN0, AINN = GND (single-ended).
- `Ain1Gnd` - AINP = AIN1, AINN = GND (single-ended).
- `Ain2Gnd` - AINP = AIN2, AINN = GND (single-ended).
- `Ain3Gnd` - AINP = AIN3, AINN = GND (single-ended).

### `Mux::code`

Returns the 3-bit field code for this multiplexer setting.

```rust
fn code(self) -> u8
```

### `Mux::from_code`

Builds a multiplexer setting from a 3-bit field code (the low three bits used).

```rust
fn from_code(code: u8) -> Mux
```

## enum `Pga`

The programmable gain amplifier's full-scale range (Config bits 11:9).

- `Fsr6_144` - Full-scale range ±6.144 V.
- `Fsr4_096` - Full-scale range ±4.096 V.
- `Fsr2_048` - Full-scale range ±2.048 V (the default).
- `Fsr1_024` - Full-scale range ±1.024 V.
- `Fsr0_512` - Full-scale range ±0.512 V.
- `Fsr0_256` - Full-scale range ±0.256 V.

### `Pga::code`

Returns the 3-bit field code for this gain setting.

```rust
fn code(self) -> u8
```

### `Pga::from_code`

Builds a gain setting from a 3-bit field code.

Codes `110` and `111` are documented as also selecting ±0.256 V and map here
to [`Pga::Fsr0_256`].

```rust
fn from_code(code: u8) -> Pga
```

### `Pga::full_scale_microvolts`

Returns the positive full-scale input voltage for this gain, in microvolts.

```rust
fn full_scale_microvolts(self) -> u32
```

## enum `Mode`

The conversion mode (Config bit 8).

- `Continuous` - Convert continuously.
- `SingleShot` - Convert once per request, then power down (the default).

## enum `DataRate`

The output data rate (Config bits 7:5), in samples per second.

- `Sps8` - 8 samples per second.
- `Sps16` - 16 samples per second.
- `Sps32` - 32 samples per second.
- `Sps64` - 64 samples per second.
- `Sps128` - 128 samples per second (the default).
- `Sps250` - 250 samples per second.
- `Sps475` - 475 samples per second.
- `Sps860` - 860 samples per second.

### `DataRate::code`

Returns the 3-bit field code for this data rate.

```rust
fn code(self) -> u8
```

### `DataRate::from_code`

Builds a data rate from a 3-bit field code.

```rust
fn from_code(code: u8) -> DataRate
```

### `DataRate::samples_per_second`

Returns the rate in samples per second.

```rust
fn samples_per_second(self) -> u16
```

## enum `ComparatorMode`

The comparator mode (Config bit 4).

- `Traditional` - Traditional comparator with hysteresis (the default).
- `Window` - Window comparator.

## enum `ComparatorPolarity`

The ALERT/RDY pin polarity (Config bit 3).

- `ActiveLow` - Active low (the default).
- `ActiveHigh` - Active high.

## enum `ComparatorLatch`

Whether the comparator latches (Config bit 2).

- `NonLatching` - Non-latching: the pin clears once readings return within the thresholds (the default).
- `Latching` - Latching: the pin stays asserted until the conversion data is read.

## enum `ComparatorQueue`

The comparator queue, or disable (Config bits 1:0).

- `AfterOne` - Assert after one conversion beyond a threshold.
- `AfterTwo` - Assert after two conversions beyond a threshold.
- `AfterFour` - Assert after four conversions beyond a threshold.
- `Disabled` - Disable the comparator and set ALERT/RDY high-impedance (the default).

### `ComparatorQueue::code`

Returns the 2-bit field code for this queue setting.

```rust
fn code(self) -> u8
```

### `ComparatorQueue::from_code`

Builds a queue setting from a 2-bit field code.

```rust
fn from_code(code: u8) -> ComparatorQueue
```

## struct `Config`

A decoded ADS1115 Config register.

Build one, set the fields, and turn it into the 16-bit register value with
[`bits`](Config::bits); or parse a register read with [`from_bits`](Config::from_bits).
[`Config::default`] is the power-on state, `0x8583`.

**Examples**

```
use pamoja_sensors::ads1115::{Config, Mux, Pga};

// Read AIN0 against ground at the ±4.096 V range, leaving everything else default.
let config = Config {
    mux: Mux::Ain0Gnd,
    pga: Pga::Fsr4_096,
    ..Config::default()
};
// The high byte then low byte are written to the Config register.
let [hi, lo] = config.bits().to_be_bytes();
assert_eq!(Config::from_bits(u16::from_be_bytes([hi, lo])), config);
```

Fields:

- `start_conversion: bool` - Start a single conversion when written; reads as "not converting" when set.
- `mux: Mux` - The input multiplexer setting.
- `pga: Pga` - The full-scale range.
- `mode: Mode` - The conversion mode.
- `data_rate: DataRate` - The output data rate.
- `comparator_mode: ComparatorMode` - The comparator mode.
- `comparator_polarity: ComparatorPolarity` - The ALERT/RDY pin polarity.
- `comparator_latch: ComparatorLatch` - Whether the comparator latches.
- `comparator_queue: ComparatorQueue` - The comparator queue, or disable.

### `Config::bits`

Assembles the 16-bit Config register value.

**Returns**

The register value to write, most significant bit first.

```rust
fn bits(self) -> u16
```

### `Config::from_bits`

Parses a 16-bit Config register value.

**Arguments**

* `bits` - the register value, as read from the device.

**Returns**

The decoded configuration.

```rust
fn from_bits(bits: u16) -> Config
```

## fn `to_nanovolts`

Converts a raw conversion result to nanovolts at the selected full-scale range.

The result is a 16-bit two's-complement code spanning plus or minus the full
scale, so the voltage is `code * full_scale / 32768`. Working in nanovolts keeps
the conversion exact in integer arithmetic across every gain setting.

**Arguments**

* `pga` - the gain the conversion was taken at.
* `raw` - the signed conversion register value.

**Returns**

The measured voltage in nanovolts.

```rust
fn to_nanovolts(pga: Pga, raw: i16) -> i64
```

## fn `to_volts`

Converts a raw conversion result to volts at the selected full-scale range.

**Arguments**

* `pga` - the gain the conversion was taken at.
* `raw` - the signed conversion register value.

**Returns**

The measured voltage in volts.

```rust
fn to_volts(pga: Pga, raw: i16) -> f32
```

